doctype html
html
  head
    title FlightDash
    link(rel='stylesheet', type='text/css', href='css/index.css')
  body
    #header
      header  My Travel Website
      nav
        ul
          li.active
            a(href='/') HOME
          li
            a(href='/history') HISTORY
          li
            a(href='/account') ACCOUNT
          li
            a(href='/about') ABOUT
          li#right
            a(href='/signin') SIGN IN
          li#right
            a(href='/signup') SIGN UP
    #placeholder
    article
      #book
        form(method='post', action='/flight', id='searchForm')
          #book-upper
            #book-upper-center
                select(name='from')
                  option(value= 0)  select
                  each item in rows
                    option(value = item.from_location) #{item.from_location} 
                select(name='to')
                  option(value= 0)  select
                  each item in rows
                    option(value = item.from_location) #{item.from_location} 
                
          #book-lower
            button(type='submit', form='searchForm', value='Submit') Submit
        //- form
        //-   .onethird  
        //-   .onethird  hello
        //-   .onethird.norightborder  hello

        //- table
        //-   thead
        //-     tr
        //-       th Flight ID
        //-       th Airline Name
        //-       th From Location
        //-       th To Location
        //-       th Departure time
        //-       th Arrival time
        //-       th Duration
        //-   tbody
        //-     each item in rows
        //-       tr
        //-         td= item.flight_id
        //-         td= item.airline_name
        //-         td= item.from_location
        //-         td= item.to_location
        //-         td= item.departure_time
        //-         td= item.arrival_time
        //-         td= item.duration

    footer
      ul
        li
        | This website is made by Eshwar, Elson, Gopan, Antony
        //- li#right
        //- | Thank You
